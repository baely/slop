# Copy to .env and set a strong password. This gates access to your log.
APP_PASSWORD=change-me

# Optional: pin visits from your real location history (Traccar / track.baileys.dev),
# the same source trackui uses. Leave blank to hide the "From tracker" button.
TRACK_TOKEN=
TRACK_DEVICE=1
