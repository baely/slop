# Local development only — production deploys via baely/infra.
services:
  movies:
    build: .
    ports:
      - "8080:8080"
    environment:
      RADARR_URL: ${RADARR_URL:-https://radarr.int.xbd.au}
      RADARR_API_KEY: ${RADARR_API_KEY}
