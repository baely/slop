# slop design system

The shared design language extracted from ~10 vibe-coded applications in this repo. Documents colors, typography, spacing, components, and layout patterns with live examples.

## View

Open `index.html` in a browser, or deploy with staticer.

## Deploy

```bash
staticer deploy --domain design.lab.baileys.dev --expires never
```
